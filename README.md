# Deployment Portal

**Version:** v4.7.8

A React 18 web application serving as a consolidated PMO-style frontend UI for the Customer Experience team to track and proactively manage risks/issues with CMs and customers. Speaks directly to HubSpot to provide real-time information on all projects in the deployment and sales pipeline. Coordinates documentation, milestones, and program details across Instrumental, Systems Integrator (SI), Customer, and CM stakeholders via a unified Project Details / Commercial / Training model.

#Vision 
The goal of this Webapp is to have a consolidated "PMO" Style frontend UI that the Customer Experience team can use to track and proactively manage risks / issues with CMs/ customers. This will directly speak to Hubspot and provide real time information on all projects in the deploment and sales pipeline 

---

## Live URL

**Primary:** https://deployment-portal-instrumental.web.app/
**Legacy (Cloud Run + IAP):** https://deployment-portal-901459055521.us-central1.run.app/

---

## Features

- **Unified project structure** — Project Details, Commercial (restricted), and Training sections per project (v3.2.0 — replaces old 4-party system)
- **Project overview dashboard** — Per-party folder/document counts and milestone progress bars
- **Checklist milestones** — OK2Contract, OK2Ship, OK2Build with checklists, signatures, and linked resources
- **Program Details** — Task and milestone timeline with drag-to-reorder
- **Customer dashboard** — Station count and key milestone dates pulled from Program Details
- **Document management** — Upload links and PDFs per folder, with per-document language tagging
- **Training section** — White / Blue / Black Belt training materials per party
- **Admin panel** — User approval, role management, restricted folder access grants, HubSpot sync control
- **Multi-language UI** — English, Español, Tiếng Việt, 繁體中文, 简体中文
- **Site status banner** — Admin-editable broadcast message shown to all users
- **Inline editing** — Admins can edit milestone descriptions and checklist labels in place
- **HubSpot CRM sync** — Auto-imports all projects from 7 HubSpot pipelines (Tue/Fri 9am PDT); admin preview + manual trigger
- **Projects Overview** — Instrumental/admin-only summary view showing all **active** projects: Demand Plan (aggregated hardware requirements), per-pipeline stage-distribution bar charts, and stage-by-stage project breakdown
- **Per-project hardware section** — HubSpot-synced hardware values shown read-only; Instrumental users can add custom manual hardware types per project
- **DB-level access control** — External users can only read projects they've been explicitly assigned to (enforced at Firebase Realtime Database rules level, not just UI)
- **Checklist templates** — New projects auto-get Internal + External checklist folders; SI projects get SI Deployment Checklist; optional apply to existing projects
- **Codename decoding** — HubSpot candy codenames automatically mapped to real customer names
- **HubSpot writeback** — Editing date fields (CAD Complete, CAD Actual Finish, Actual Service Start, Target Build, Actual Deploy) in Project Overview patches the HubSpot custom object record automatically on save, with "↑ Syncing / ✓ HubSpot updated / ⚠ failed — saved locally" indicator
- **`si_admin` role** — New role between `user` and `admin`; grants edit access to the All SI Projects tracker without full admin powers
- **All SI Projects view** — Sidebar sub-tab under All Projects Overview (Instrumental-visible, si_admin-editable); shows only SI Partner Deployment pipeline projects with per-project risk flag, last contact, next milestone, notes, and custom columns; includes the SI Kanban at the top
- **Sidebar sub-navigation** — Project Details categories listed as collapsible sub-items in the left sidebar; external users see only Design Specs & Integration Docs, CAD & Drawings, and Hardware & MES Deployment Requirements
- **Non-PDF file uploads** — Upload button extended to DOCX, XLSX, PPTX, images, CSV, and `.lbx` label files (50 MB cap); per-file icon by type
- **Gantt chart toggle** — "📊 Show Gantt Chart" button on Project Overview; disabled with tooltip if < 3 dates available
- **HubSpot project hyperlinks** — 🔗 icons on project names in Overview, Manage Projects, and All Projects; open HubSpot record in new tab
- **Scheduled maintenance** — Runs Tue & Fri at 3 PM PT; sweeps old sync/audit/writeback logs; evaluates 3 agentic rules (bug threshold, sync error rate, circuit breaker); results in Admin Panel → 🔧 Maintenance tab
- **Pre-uploaded deployment requirement docs** — 10 master files auto-linked into every project's "Hardware & MES Deployment Requirements" folder at first open; external users can download
- **Performance** — Code-split bundle (react-vendor + firebase-vendor chunks); function memory bumps; memoized heavy views
- **Shipment Details sync** — HubSpot sync auto-populates `item_num` rows in each project's Shipment Details tab from associated HubSpot Shipments records (INxxx); no duplicates; other columns left blank for manual fill-in

---

## Tech Stack

| Layer | Technology |
|-------|-----------|
| Frontend | React 18 + Vite |
| Database | Firebase Realtime Database |
| Auth | Firebase Authentication (Google OAuth) |
| Hosting | Firebase Hosting |
| Cloud Functions | Firebase Functions (Node 20) — HubSpot sync, AI Bot (Claude), admin callables, provisioning |
| AI | Anthropic Claude API (`claude-sonnet-4-20250514`) — Project Bot, drafted project status |
| CRM | HubSpot Custom Objects API (v3), 7 pipelines |
| Legacy hosting | Docker + Nginx on Google Cloud Run (with IAP) |
| Styles | Inline styles only (no CSS files) |

---

## Project Structure

```
firebase-project/
├── src/
│   ├── App.jsx           # Entire application (single file)
│   ├── main.jsx          # React entry point
│   ├── firebase.js       # Firebase SDK init (auth, db, functions)
│   └── hubspotConfig.js  # Pipeline/stage definitions, codename map
├── functions/
│   ├── index.js          # Cloud Functions: HubSpot sync, checklist template
│   ├── checklists.js     # Checklist template data (Internal/External/SI)
│   └── package.json      # Node 18, firebase-admin, node-fetch
├── data/                 # Source CSV files (codename decoder, checklists)
├── public/
├── firebase.json         # Firebase Hosting + Functions config
├── .firebaserc           # Firebase project + hosting target
├── database.rules.json   # Firebase Realtime Database security rules
├── Dockerfile            # For Cloud Run deployment
├── nginx.conf            # Nginx config for Docker container
├── vite.config.js
└── package.json
```

---

## Local Development

```bash
# Install client dependencies
npm install

# Start dev server (http://localhost:5173)
npm run dev

# Install Cloud Functions dependencies (one-time)
cd functions && npm install && cd ..
```

### Functions `.env` (one-time setup — do NOT commit)

Create `functions/.env`:

```
HUBSPOT_TOKEN=pat-na2-YOURTOKEN
ANTHROPIC_API_KEY=sk-ant-YOURKEY
```

`.env` is gitignored. The legacy `firebase functions:config:*` API is deprecated — we use `process.env` in `functions/index.js`.

---

## Deployment

### Firebase Hosting (primary)

```bash
npm run build
npx firebase-tools deploy --only hosting:deployment-portal-instrumental
```

### Cloud Functions (HubSpot sync)

```bash
cd functions && npm install && cd ..
npx firebase-tools deploy --only functions
```

### Cloud Run (legacy — keeps IAP protection)

```bash
docker build -t gcr.io/deploymentportal-5ec3a/deployment-portal .
docker push gcr.io/deploymentportal-5ec3a/deployment-portal
gcloud run deploy deployment-portal \
  --image gcr.io/deploymentportal-5ec3a/deployment-portal \
  --platform managed \
  --region us-central1
```

### One-time Firebase Hosting setup (already done)

```bash
npx firebase-tools login
npx firebase-tools hosting:sites:create deployment-portal-instrumental
npx firebase-tools target:apply hosting deployment-portal-instrumental deployment-portal-instrumental
```

---

## Access Control

| Role | Who | Access |
|------|-----|--------|
| SuperAdmin | One designated user (`superAdmin: true` in DB) | All admin powers + can promote others to admin |
| Admin | Explicitly granted by SuperAdmin | Full read/write, user management, all parties |
| SI Admin | Granted by Admin (`role: "si_admin"`) | Edit SI tracker; same Instrumental read-access elsewhere |
| User — Instrumental | Auto-provisioned `@instrumental.com` | All projects read + write for Instrumental-owned fields |
| User — External | Manually approved by admin | Assigned projects only; read-only for Design Specs, CAD, Deployment Docs |

New users sign in with Google and land in a pending queue until an admin approves them and assigns a party.

---

## Firebase Project

**Project ID:** `deploymentportal-5ec3a`
**Hosting site:** `deployment-portal-instrumental`
**Database:** Firebase Realtime Database (default)

---

## Version History

| Version | Description |
|---------|-------------|
| v4.7.8 | **Fixed the real cause of missing Station Kit serial numbers: our own silent HubSpot batch failures, not a HubSpot data gap. 30% of all synced hardware was corrupted.** v4.7.7's coverage report claimed "10 project(s) had kits but ALL slots empty (HubSpot data gap)". Investigation of all 10 against the HubSpot API proved that label wrong on both count and cause. **What was actually happening**: `fetchComponentsForKits` fanned out every fleet-asset property batch-read at once via unbounded `Promise.all` (~40 concurrent requests on that step alone, on top of the rest of the sync), each call site swallowed failures with a bare `if (!res.ok) return;`, and there was **no retry, no backoff, and no `Retry-After` handling anywhere in the file** (`FETCH_TIMEOUT_MS = 15000`). When a batch timed out or got rate-limited, `compProps` stayed empty for up to 100 assets, and the line `serial: p.asset_sn \|\| p.name \|\| assetId` fell through to its last fallback — **writing the HubSpot object ID into RTDB as if it were a serial number**, with `category` empty so `normalizeCategory` returned `"Other"` and no slot mapping occurred. **Measured blast radius** (full scan of `appState/docData`): **1,503 of 4,965 HubSpot-sourced `_hardwareTracking` items (30.3%) across 243 of 308 projects** carried an object ID in the serial field — e.g. Nerds FXHC (`hs_324076600005`) showed all 17 items as `type: "Other"` with serials like `23275122555`, when HubSpot had clean `category__c` values (5× `Camera 20MP USB`, `Computer / Desktop`, `Telecentric Lenses`, 5× `Barcode Scanner`, 5× `LED Light Controller`) and real serials the whole time. Re-reading all 900 unique affected asset IDs returned HTTP 200 with serial + category for **900/900** — the data was never missing; we dropped the responses and never logged it. Users were reading object IDs off the Station Kits table as if they were serial numbers. **Fixes**: (a) new `hubspotFetch(url, options, label)` wrapper — retries 429/5xx/timeout with exponential backoff (500ms → 1s → 2s, 4 attempts), honors `Retry-After`, returns `null` only after logging `GAVE UP … last error`, and short-circuits non-retryable 4xx with the response body in the log. (b) new `mapLimit(items, limit, fn)` bounded-concurrency map replaces `Promise.all` over all batches; `HS_BATCH_CONCURRENCY = 6`. (c) **removed the `\|\| assetId` serial fallback** — assets whose properties didn't resolve are now flagged `unresolved: true` and carry `serial: ""`; a missing row is honest, a fabricated serial is not. (d) `buildHardwareFromKits` indexes the previous sync's items by `_fleetAssetHubspotId` and carries the last known-good row forward (tagged `_staleFromFailedSync`) when a read fails — but treats a stored object-ID serial as non-reusable so corrupted rows get repaired rather than perpetuated. (e) `buildStationKitsRowsFromKits` filters unresolved components out of slot assignment so an empty placeholder can't occupy `camera_1_sn` ahead of a real component. (f) the `pd_station_kits` merge now detects a degraded run (any unresolved component, or a kit whose props batch failed — `fetchKitsForProjects` now returns `failedKitPropIds`) and **refuses to blank a populated slot** with an empty value, while still applying genuine HubSpot removals on a clean run. (g) `fetchKitsForProjects` hardened the same way for project→kit and kit-props reads, so a dropped batch no longer makes projects look kit-less or wipes `fixture_name` / `station_name`. (h) **coverage line no longer asserts a cause it cannot distinguish** — now reports `unresolved_components:N/M (our fetch failed)` and `kits_with_no_fleet_assets:N/M (nothing associated in HubSpot)` separately from the symptom list, plus a `DEGRADED RUN` error line when any component fails to resolve. **Verified**: `node --check` clean; `npm run build` clean; 10/10 helper smoke tests pass against the live HubSpot API (object-ID detection, concurrency cap of 6, order preservation, real 200 resolving `23275122555` → `INST01014`, non-retryable 400 and 401 both returning `null` in <3s without retrying). **Genuine ops task that remains** (the real, much smaller A5): 7 of the 10 originally-flagged projects have a kit with **zero** fleet assets associated in HubSpot — Lot100, Palo Alto Networks, Nerds/Sunnyhills, Ring Addendum #2, ASUS Vera Rubin, GIGABYTE VR/GB, Ring Lazarus. Nothing to sync there until ops attaches assets. **Still unhardened** (out of scope, same class of bug): 4 remaining bare `if (!res.ok) return;` sites in the notes batch-read, HubSpot Files, and Shipments (×2) paths. |
| v4.7.7 | **Systematic Station Kits / Fleet Asset SN coverage fix — reverses v4.6.0 active-only filter + auto-creates missing `pd_station_kits` inline during sync + per-sync coverage report.** Latest sync before v4.7.7 processed 798 total projects: **621 skipped** (my v4.6.0 active-only filter), 177 reached the sync step, 29 had HubSpot kit associations, but only **13 got `pd_station_kits` row updates** — the other 16 had `_hardwareTracking` populated yet were silently skipped at [functions/index.js:1304](functions/index.js#L1304) because their `projectDetails` didn't contain a `pd_station_kits` category. Root causes: (1) my v4.6.0 filter reversed; user now wants completed projects to also resync when ops adds hardware post-deployment. (2) Projects created before `pd_station_kits` was in the standard template silently miss the table during sync (though `TABLE_TEMPLATES` in `functions/checklists.js` DID include it — it just relied on someone triggering `ensureProjectTemplate` or `backfillDeploymentDocs` per project). **Changes**: (a) [functions/index.js:1252](functions/index.js#L1252) — removed `activeIncoming = incoming.filter(p => p.status === "active")`; sync now processes ALL projects (active + closed). Log line updated: `[stationKits] syncing all N project(s) (active + closed)`. (b) [functions/index.js:1303](functions/index.js#L1303) — replaced silent `if (skCatIdx === -1) continue;` with an inline auto-create using the `TABLE_TEMPLATES` template (matches Alek's pd_cad auto-create at line 1417-1423 and pd_meeting_notes at line 1502-1506). New log: `[stationKits] {pid}: auto-created missing pd_station_kits table`. (c) End of stationKits sync block — added a coverage report emitting a single log line per sync: `[stationKits] COVERAGE: N project(s) synced; M kit row(s); computer_sn:X/M camera_1_sn:X/M lens_1_sn:X/M; Z project(s) had kits but ALL slots empty (HubSpot data gap): pid1, pid2, ...`. This distinguishes sync bugs from HubSpot-data-quality issues at a glance — going forward, "SNs missing on project X" is answerable with one grep of Cloud Function logs. **Scope**: Non-SI projects only (Instrumental Hardware Deployment pipeline + `[SI]`-tagged Hardware Deployment projects — both live in `appState/projects` and use `pd_station_kits`). SI Partner Pipeline projects (`appState/siProjects`) unchanged — they use a different schema with a `stations` map that has no HubSpot fleet asset SN wiring. Adding it there is a future v4.8+ scope item. **Compatibility**: builds on v4.7.6's `category__c` fix; no data-migration needed since the sync auto-creates on next run. `backfillDeploymentDocs` (admin-triggered CF) will inject `pd_station_kits` on all projects at once if needed via its existing `TABLE_TEMPLATES.forEach` iteration — no code change to that CF required. |
| v4.7.6 | **Real fix for fleet asset SN sync (correct HubSpot property `category__c`) + git↔prod reconcile after v4.7.5 rollback.** v4.7.5 attempted to fix the fleet asset SN issue by adding an `associationLabel` fallback in `buildHardwareFromKits`, but ALSO introduced a UI regression in the checklist export modal that broke `ProjectTabsView` (Station Kits tab rendered empty). Prod hosting was rolled back via Firebase Console to Alek's `abef2d` — leaving prod hosting on Alek's clean code, prod functions on my v4.7.5 CF changes, and git ahead of both. Also: v4.7.5's fleet asset fix was INCOMPLETE — the fallback relied on `associationLabel` being present, but for many projects (like VR-SJ) HubSpot has no labels set on kit ↔ fleet asset associations. Direct HubSpot API investigation revealed the actual data lives on a **fleet asset property named `category__c`** (label: "Category (Master)") — with values like `"High Res Camera"`, `"Line Scan/Large Format Lens"`, `"Computer / Desktop"`. My v4.7.5 code queried `category_master` and `category` — neither of which exist as API names. **Fix**: (a) [functions/index.js `fetchComponentsForKits`](functions/index.js) now requests `category__c`, `asset_category_new`, `model__c` (real HubSpot API names) alongside the legacy names, plus `model_number` / `model` as fallbacks; component object's `category` field now sources from `p.category__c || p.asset_category_new || p.category_master || p.category` in priority order. (b) [functions/index.js `buildStationKitsRowsFromKits`](functions/index.js) now maintains a category-based bucket (`byCategory.camera`, `.lens`, `.computer`, `.monitor`, `.barcode`, `.led`) alongside the existing `byLabel` map. Uses `normalizeCategory` to classify each component from its `category` field, then a `pick(labelKey, catBucket)` helper prefers labeled matches when available, else falls back to first-of-category. Result: projects WITH labeled associations (Fundip, Nerds, etc.) work identically to v4.7.5; projects WITHOUT labels (VR-SJ, SunnyHills, and similar) now populate `camera_1_sn` / `lens_1_sn` / `computer_sn` / etc. from category. Limitation: for multi-component kits (e.g., 3 cameras) without labels, only slot 1 is filled — labels are still needed to disambiguate. `no_cameras` count now uses labeled count OR falls back to `byCategory.camera.length`. **Git ↔ prod reconcile**: `src/App.jsx` reset to Alek's `d214802` state (removed my broken checklist export modal + button changes). No visible UI change in this release — just the invisible-until-you-look-at-it fact that fleet asset SNs populate correctly on the next sync. Explicitly deferred to v4.7.7: (a) diagnose why my v4.7.5 checklist modal broke `ProjectTabsView` (likely a hook/JSX interaction we didn't trace); (b) safely re-ship the checklist export improvements (picker modal + better docx format). |
| v4.7.5 | **Fleet asset SN sync fix + checklist export rebuilt with picker + Alek's 12 bundled commits (AI agent, stage dates, SIRD, hooks, QA guide, WCAG a11y).** **(1) Fleet asset SNs weren't showing in `pd_station_kits`** — despite the sync running successfully and RTDB `_hardwareTracking` containing real serials (e.g., VR-SJ project `hs_317171597032` had 6 rows with serials like `INSTUS8510`, `INSTUS9356`), the UI showed nothing. Two bugs in [functions/index.js](functions/index.js): (a) `buildHardwareFromKits` at line 605 set `type: normalizeCategory(comp.category)` — but HubSpot's `category_master` / `category` field is empty on most fleet assets, so ALL components ended up as `type: "Other"`. Fix: fall back to `associationLabel` (`normalizeCategory(comp.category || comp.associationLabel)`) — the labeled-typeId capture in `fetchComponentsForKits` reliably provides labels like `"camera 2"`, `"Lens 3"`, `"Computer"`. (b) `buildStationKitsRowsFromKits` at lines 636-652 used exact case-sensitive lookup (`byLabel["Camera 1"]?.serial`), but HubSpot returns labels in inconsistent casing (`"camera 2"` lowercase vs `"Camera 1"` capitalized vs `"camera_1_sn"` snake). Fix: introduced `normKey(s)` = lowercase-alphanumeric-only, normalized both the byLabel map keys and the lookup calls (`byLabel["camera1"]?.serial`, `byLabel["computer"]?.serial`, `byLabel["ledlightcontroller"]?.serial`, etc.). After next sync, `pd_station_kits` will populate `computer_sn`, `camera_1_sn`, `lens_1_sn`, `monitor_sn`, `led_controller_sn`, `barcode_scanner_sn` for all projects with labeled kit associations. Manually-added rows and manual columns still preserved via the existing allow-list merge in runSync. **(2) Checklist export completely rebuilt** ([src/App.jsx `handleChecklistExport` + `handleChecklistExportDocx`](src/App.jsx)) — v4.7.3/v4.7.4 shipped separate Excel and Doc buttons that always exported ALL checklists with basic formatting. Feedback: format looked bad + no way to pick which checklists (Internal / External / MES / etc.). Now: single **📋 Export Checklists…** button opens a modal picker with (a) format radio: `📄 Word / Google Doc (.docx)` or `📊 Excel / Google Sheet (.xlsx)`; (b) checkbox per checklist category showing completion status (`Internal Checklist — 12 / 45 complete (27%)`); (c) Cancel / Export buttons. Modal defaults: all checklists selected + Doc format. Both export functions accept a `selectedIds` filter. **Doc format rebuilt**: (i) removed all emojis from cell content (they render inconsistently in Word) — status now `Complete` / `Pending` / `N/A` in green/amber/grey bold; (ii) added explicit thin gray borders on every cell (Word defaults to invisible borders); (iii) alternating row shading (zebra stripes at `#F8FAFC` on odd rows); (iv) explicit Calibri font family + explicit sizes (10pt body, 11pt headers, 24pt title); (v) 0.5" page margins for max table width; (vi) placeholder `—` in empty cells with muted color instead of blank; (vii) cleaner header block (project name 24pt bold + "Deployment Checklists" 14pt muted + metadata line separately); (viii) milestone headers as two-run paragraphs (bold name + italic muted count) instead of concatenated string; (ix) cell padding tuned for readability. Excel export also accepts `selectedIds`. **(3) Alek's 12 upstream commits** now in main history and deployed: `d214802` restore user prop in SIProjectDetail (fixed a merge regression from my v4.7.2), `379f850` massive AI agent + stage dates + SIRD + hooks fix + HubSpot sync improvements (+2.5k lines), `14fa25d` QA guide + pre-deploy checklist in CLAUDE.md, `0f812d8` bidirectional FAT/SAT planned date sync, `9d7bc68` Stage Dates above Danger Zone in slide-out, `730d573` end date calendar opens at start month, `0f3f57b` timeline hover tooltips + pan/zoom + slider, `b3dcf99` SlideStageDates live-Firebase sync, `941d4ba` show planned bars by default + auto-save slide-out overview, `b56855f` always show stage dates at bottom of SI slide-out, `138975d` force SIProjectSlideOut remount on project change. **(4) Alek's `index.html` WCAG a11y additions preserved**: `:focus-visible` outline ring, `prefers-reduced-motion` disable-animation media query. Function deps `mammoth` and `xlsx` added by Alek in `functions/package.json` also preserved. |
| v4.7.4 | **Checklist export also available as .docx (Google Doc / Word) — 📄 Export Checklists (Doc).** Complements v4.7.3's Excel export with a Word-compatible document format that uploads to Google Drive as a Google Doc. Reuses the `docx` library (was already a dep — v4.6.0 hotfix by Alek installed it). **Structure**: (a) Title paragraph with project name + " — Deployment Checklists"; (b) subtitle showing export date + overall completion `X / Y complete (P%)`; (c) per-checklist Heading 1 with completion summary; (d) per-milestone Heading 2 with `Section Name — N/M complete`; (e) milestone description in italic grey below the heading; (f) formatted table per milestone with 7 columns: `# | Task | Status | Owner | Target | Completed | Notes`. **Table styling** (works in Word / Google Docs / Pages): (i) dark navy header row (`#1E293B`) with white bold text; (ii) column widths tuned as percentages (Task 42%, Notes 8%, dates 10% each, etc.); (iii) status cell centered; (iv) status rendered as `✅ Complete` / `⏳ Pending` / `N/A`. New imports pulled from `docx` package: `Table`, `TableRow`, `TableCell`, `WidthType`, `AlignmentType`, `BorderStyle` (were previously unused; already in dep tree). Old Excel export renamed to **📋 Export Checklists (Excel)** and new one added as **📄 Export Checklists (Doc)**. Filename: `{project-name}_checklists_{YYYY-MM-DD}.docx`. **How to open as Google Doc**: upload the .docx to Google Drive → right-click → "Open with Google Docs" → save as native Google Doc if desired. Or open directly in Word / Pages. |
| v4.7.3 | **Customer-facing checklist export (📋 Export Checklists).** The existing 📤 Export to Spreadsheet button ([src/App.jsx `handleGlobalExport`](src/App.jsx)) filters to `cat.type === "table"` only — it silently skips checklists, so users trying to export a project's checklist state got just the tables (Team, Shipment Details, Station Kits, etc.). **Fix**: added a new **📋 Export Checklists** button next to it that runs `handleChecklistExport` — filters to `cat.type === "checklist"` and produces a customer-facing `.xlsx` with one sheet per checklist category (Internal Checklist, External Checklist, MES Integration Checklist, etc.). **Per-sheet layout**: (a) title row with the checklist name merged across all columns; (b) summary row showing `Project: X  |  Completion: N/M (P%)  |  Exported: YYYY-MM-DD` also merged; (c) frozen 4-row header (title + summary + spacer + column headers) so scrolling the item list keeps the header pinned; (d) columns: `#  |  Task  |  Status  |  Owner  |  Target Date  |  Completed Date  |  Completed By  |  Notes` with tuned widths (60ch for Task, 40ch for Notes, 14ch for dates); (e) each milestone becomes a merged section header row with `Section Name — N/M complete · description`; (f) items listed below with status rendered as `✅ Complete` / `⏳ Pending` / `N/A` for readability in Excel/Google Sheets; (g) blank spacer row between milestone sections; (h) auto-filter on the data range so customers can filter by owner or status inline. Output filename: `{project-name}_checklists_{YYYY-MM-DD}.xlsx`. Old export button renamed to **📤 Export Tables** to make the two options unambiguous. Uses SheetJS Community (already a dep) — no new packages; cell font/color styling remains basic since that's Pro-only, but layout + freeze pane + merges + widths + filter give a clean deliverable customers can consume. |
| v4.7.2 | **Per-project AI Bot chat now available in the SI drill-in.** Complements v4.7.1's global bot bar. Users can drill into an SI project (via All SI Projects → click row) and get a floating **AI** button in the bottom-right that opens a chat scoped to THAT SI project. **(1) `ProjectBotChat` extended with `isSIProject` prop** ([src/App.jsx `ProjectBotChat`](src/App.jsx)) — new `buildCallData` helper switches between `projectId` (regular projects, reads `appState/projects` + `appState/docData`) and `siProjectId` (SI projects, reads `appState/siProjects` — completely different schema). Empty-state suggested prompts adapt: SI shows `["What's the current stage?", "Summarize station deployment status", "What files do we have and any gaps?"]`; hides the "Fill: Hardware/Program Details/Deployment Planning" buttons (SI projects don't have those sections). Header text updates to "I can see stations, stage dates, files (SIRD/FAT/SAT), inspection status, and recent Slack activity" for SI. **(2) `askProjectBot` CF branches on `siProjectId`** ([functions/index.js `askProjectBot`](functions/index.js)) — new optional payload param `siProjectId` triggers reads from `appState/siProjects/{pid}` instead of `appState/projects/{pid}` + `appState/docData/{pid}`. Skips the checklist/hardware/program-details context blocks (SI projects don't have those in docData); instead flattens SI-specific fields into the prompt: SI Partner (`si_name`), SI PM (`si_pm`), Customer, CM Site, Factory Location, Current Stage, Drive URL, Stage Dates (planned + actual for each stage), Stations list (station_number, name, deployment_factory, count, FAT/SAT results), File groups (SIRD/FAT/SAT/etc. with item counts), Inspection status. Normalizes SI project into shape compatible with `fetchSlackContext` (uses `si_name` slug candidate added in v4.7.1). All Slack + sibling + attribution rules from v4.7.0 still apply. **(3) `SIProjectDetail` accepts `user` prop** ([src/App.jsx `SIProjectDetail`](src/App.jsx)) — threaded through from `AllSIProjectsView` (which had `user` all along); renders `<ProjectBotChat project={{ id: pid, name: project.name }} user={user} isSIProject={true} />` at the end of the drill-in view. No new component created — reuses ProjectBotChat UI 100%. **(4) `siFullscreen` gate at [src/App.jsx:11893](src/App.jsx#L11893) remains** for the outer `ProjectBotChat` — the SI drill-in gets its OWN inner bot instead. This keeps the outer bot's `project` prop (from `appState/projects`) from confusing users when they're viewing an SI project (`appState/siProjects` — different schema). **Backlog for v4.7.3+**: Slack context quality iteration (test with real SI channels), rotate Slack Bot Token, revisit whether to also enable `ProjectBotChat` at the outer level for SI (currently blocked because outer `project` state is undefined during SI view). |
| v4.7.1 | **AI Bot now available in the SI section + SI projects in global bot fuzzy-match + Slack channel resolution via `si_name`.** Pre-v4.7.1, when a user opened the All SI Projects view (which fires `siFullscreen = true`), all three components sharing that gate at [src/App.jsx:11886-11893](src/App.jsx#L11886-L11893) were hidden: the main Sidebar, the "Ask AI anything" `GlobalBotBar`, and the per-project `ProjectBotChat`. SI users had zero conversational AI. **(1) Ungate `GlobalBotBar` for SI view** — removed the `!siFullscreen &&` gate at [src/App.jsx:11890](src/App.jsx#L11890) so the "Ask AI anything" bar renders in every view including SI. Left `Sidebar` (11886) and `ProjectBotChat` (11893) gated for now: sidebar hiding is intentional for SI fullscreen mode, and `ProjectBotChat` requires a `project` from `appState/projects` which the SI drill-in doesn't set (v4.7.2 target — needs a dedicated `SIProjectBotChat` rendered inside `AllSIProjectsView` at the drill-in level, or `askProjectBot` branching on `appState/siProjects`). **(2) `askGlobalBot` now includes SI projects.** [functions/index.js `askGlobalBot`](functions/index.js) reads `appState/siProjects` alongside `appState/projects` in the initial Promise.all, normalizes each SI project into a `{id, name, customer, codename, si_name, hubspotStageLabel, stations, isSI, _isSIRecord}` shape (SI schema is completely different — `stations` is a keyed map not a count, no `checklists`/`hardware`, has `si_name`/`si_pm`/`cm_site`/`current_stage`/`stage_dates` instead), and includes them in the fuzzy-match search. Search now also includes `p.si_name` in the searchable string so users can ask about SI partners by name (e.g., "what's Anda working on"). Prompt output: SI projects get a distinct `[SI]` prefix + partner-focused one-liner (`SI partner: X, customer: Y — stage: Z; stations: N`) so Claude can distinguish them from deployment projects. Active SI project cap: top 40 (skipped if already in `mentionedProjects`). Total prompt: up to 100 active deployment + 40 active SI + 2 mentioned. **(3) `fetchSlackContext` tries `si_name` as an additional slug candidate.** Previously slugified only `codename` and `customer` fallback. Now also tries `si_name` — helps SI projects (which often lack `codename` or `customer` on their linked deployment record) find their partner-associated channels if any exist (`#customer-{si_name-slug}` pattern). Fails gracefully as before if no channel matches. Log line updated to show all three source values so debugging shows exactly what was tried. **Backlog for v4.7.2**: dedicated `SIProjectBotChat` component rendered inside `SIProjectDetail` (src/App.jsx:6008), reading SI project data from `appState/siProjects` (stations/files/inspection/stage_dates) instead of relying on the outer `project` state; either a new `askSIProjectBot` CF or branching logic in `askProjectBot` to handle the SI schema. Also: rotate the Slack Bot Token (was pasted in v4.7.0 session transcript). |
| v4.7.0 | **Instrumental-user visibility fix + AI Bot Slack context + Node 22 upgrade + Nic's design tokens adopted.** **(1) Bug fix — Wen (and other `@instrumental.com` users with `role: "user"`) couldn't see completed projects.** Root cause at [src/App.jsx:11836](src/App.jsx#L11836): the sidebar project filter used `user.role === "admin"` to bypass `user.projects` gating, but instrumental users have `role: "user"`. They received full RTDB data (per [src/App.jsx:11631](src/App.jsx#L11631) which correctly uses `isAdminOrInst`) but the UI narrowed the visible list to only projects explicitly in `user.projects` — dropping inactive/completed ones. Since `inactiveCount` was then 0, the "Include completed" toggle never rendered. Fixed both this line and [src/App.jsx:11813](src/App.jsx#L11813) (auto-select initial project) to use the module-level `isInst(user)` helper, which correctly covers admin + `@instrumental.com` email. Audit also confirmed [11631, 11666, 11684, 11841] remain intentionally admin-only (RTDB subscriptions, admin panel data, commercial-tab gate). **(2) AI Bot gets Slack customer-channel context — with intelligent multi-project disambiguation.** ([functions/index.js `fetchSlackContext`](functions/index.js) helper + injection in **both** `askProjectBot` (per-project "Ask Bot to draft" + project chat) **and** `askGlobalBot` (the "Ask AI anything" sidebar bar). New env var: `SLACK_BOT_TOKEN` in `functions/.env`. Slack app scopes required: `channels:read`, `channels:history`, `groups:read`, `groups:history`, `users:read`, `users:read.email`, `channels:join`. **How channel resolution works**: slugifies project's `codename` (Slack channels use codenames, not decoded customer names — verified during testing), searches bot's channel memberships via `users.conversations` (NOT `conversations.list`, which unreliably returns member channels for bot tokens); if bot isn't a member of any matching `#customer-{slug}*` channel, uses `channels:join` scope to auto-join a matching public channel (falls back gracefully if scope missing or channel is private). Prefers exact `customer-{slug}` match over suffixed variants. **Multi-project disambiguation**: customer channels typically cover many projects for the same customer (e.g., `#customer-sunnyhills` covers GB300, Vera Rubin at San Jose, Vera Rubin at Taoyuan, VR Connector Inspection, Midplane, etc.). Naive keyword filtering fails because sibling projects share tokens (`Vera Rubin` at SJ vs at Taoyuan share `vera`, `rubin`; multiple projects share `san`, `jose`, `sj`). Solution: fetch 40 recent messages, include ALL of them in the Claude prompt with rich sibling metadata (each sibling's name + status + stage + station count + SI flag), location-shorthand legend (`SJ = San Jose, TW = Taiwan/Taoyuan`), and explicit reasoning hints so Claude can disambiguate per-message using product/station name, location, station count, and mentions. **Prompt rules injected**: (a) attribute cited messages to author + date; (b) never fabricate quotes; (c) sibling messages must not be attributed to the target; (d) if channel activity is ambiguous, ASK the user a clarifying question ("I see activity that could apply to multiple projects — did you mean X or Y?") rather than dismissing; (e) always ground responses in a date, even if the last known activity is weeks old — never say "no updates" bare; (f) prefer surfacing information over withholding. **Author resolution**: `users.info` batch-called per unique message author, replaces raw `<@UXXX>` mentions in message bodies with real names. **Global bot enhancement** ([functions/index.js `askGlobalBot`](functions/index.js)): question is fuzzy-matched against project name/codename/customer/HubSpot ID; top 2 mentioned projects (with score ≥2) get FULL Slack context pulled + injected under `=== SLACK CONTEXT FOR REFERENCED PROJECTS ===`. Cap on active-project summary raised from 60 to 100; mentioned projects are always included in the summary even if inactive/completed. Fails gracefully — no token / no channel / API error → bot runs without Slack context. **(3) Node 20 → Node 22 runtime upgrade** — `functions/package.json` `engines.node: "22"`. Beats the Firebase decommission deadline (2026-10-30). Kept `firebase-functions` at ^4.9.0 for now; upgrade to 5.x/6.x deferred to v4.8.0 (requires migrating 29 CFs from v1 `functions.runWith().https.onCall()` to v2 API — bigger project). **(4) Adopted Nic's design tokens.** Amy's `tokens.css` from the [Design System Adoption Kit](https://drive.google.com/drive/folders/1rSVUDF-5FXNJC5UqoeZ_-HzmvzfAT0-Q) dropped into `src/styles/tokens.css`, imported once at `src/main.jsx`. `index.html` gained Roboto + Roboto Mono (Google Fonts) + Tabler icon webfont (jsDelivr CDN) `<link>` tags. Introduces `--color-primary` (Instrumental purple `#93248E`), `--color-secondary` (Salesforce blue), `--gray-0..9` scale, `--space-xs..2xl`, `--radius-*`, `--shadow-card`, `--bg-*`, `--text-*`, `--header-height`, `--sidebar-width` CSS variables + `.ds-card`, `.ds-btn`, `.ds-btn-primary`, `.ds-app-header`, `.ds-app-sidebar`, `.ds-chip`, `.ds-status-*` primitive classes. Tokens are now available app-wide; migrating existing inline-`S`-object hex codes to `var(--...)` references is a gradual follow-up (existing UI unchanged in v4.7.0 — token adoption is the foundation for the follow-up polish). |
| v4.6.0 | **Search bar overhaul + closed-project sync filter + 5 bundled fixes from Alek Tyszka.** **Alek's fixes (rebased under v4.6.0 during merge)**: (a) `feat`: Timeline auto-expand SI groups on first load + added `CLAUDE.md`. (b) `fix`: open-detail routed to wrong project when clicking a search result; sync button now shows in all tabs; renamed "HubSpot Sync" → "Sync with HubSpot" for consistency. (c) `fix`: install missing `docx` dependency (regression from v4.5.8 — the app was silently failing to generate .docx files). (d) `fix`: auto-stub creation checks `autoPid` and `hubspot_id` before falling back to name matching (prevents duplicate project stubs when HubSpot IDs are known). (e) `fix`: P-number matching in `findLinkedSiProject` only matches at the start of the core name (was previously substring-matching, causing false positives like matching "P3" against "P30"). **Asang's changes (search + sync work)**: response to manager's feedback that the search bar wasn't "pulling all the projects" and a question about whether station kits were being re-synced for completed projects. **(1) Multi-field search** ([src/App.jsx:991-1020](src/App.jsx#L991-L1020) sidebar, [src/App.jsx:5382-5395](src/App.jsx#L5382-L5395) SI tracker): both search bars now match `name`, `hubspotId`, `customer`, `codename`, and `hubspotStageLabel` (SI tracker also matches `si_name` + effective stage). Previously matched `name` only via `.includes()`, so searches like the HubSpot ID `316447023841` or a customer name like "Acme" returned nothing if it wasn't in the project name. **(2) "Include completed" toggle persistence + non-admin exposure**: moved from `sessionStorage.v454_show_inactive` → `localStorage.dp_show_inactive` so the choice survives sign-outs and new sessions; admin-only gate removed — non-admins now see the toggle if any of their assigned projects are inactive (e.g., they completed a deployment and want to look it up). **(3) Hidden-by-filter hint**: when a sidebar search returns 0 results AND there are matching completed projects hidden by the toggle being off, a yellow "📦 N completed projects match — click to include completed" button appears in the dropdown; one click flips the toggle on. **(4) Station kits no longer re-sync for closed projects** ([functions/index.js:1112-1116](functions/index.js#L1112-L1116)): added `incoming.filter(p => p.status === "active")` before computing `projectHsIds` for `fetchKitsForProjects`. Closed projects' existing `_hardwareTracking` and `pd_station_kits` rows stay in RTDB (readable, editable, writes-back-to-HubSpot still work) but are no longer re-fetched on each sync, saving HubSpot API quota + RTDB writes. Sync logs the filter count: `[stationKits] filtering to active projects: N of M (skipping K closed)`. Other sync blocks (DRIs, CAD docs, Notes, Shipments) intentionally unchanged — those are cheaper and benefit from staying current even after closure. |
| v2.0.0 | Baseline — multi-party portal with milestones, documents, training, admin panel |
| v2.1.0 | Firebase Hosting URL, multi-language UI, Instrumental/Customer dashboards, milestone % progress, inline editing |
| v2.1.1 | Chronological milestone sort, drag-to-reorder for Program Details and Checklist Milestones |
| v2.2.0 | Language dropdown moved to sidebar bottom, clickable party cards, item count fix, superAdmin role, @instrumental.com no longer auto-admin |
| v2.2.1 | Fixed broken IIFE in overview party cards — non-admin users can only navigate to their own party |
| v3.0.0 | HubSpot CRM sync (6 pipelines, auto Tue/Fri 9am + manual), Projects Overview tab, checklist templates (Internal/External/SI), codename decoding, admin panel HubSpot sync UI, Instrumental/External user split |
| v3.1.0 | DB-level access control, Demand Plan, per-pipeline bar charts, per-project hardware section |
| v3.2.0 | Remove 4-party system, unified Project Details/Commercial/Training, new checklist template, searchable dropdown |
| v3.3.0 | Security lockdowns, Manage Projects overhaul, SI Kanban, Gantt chart, hardware demand forecast, App Scripts links, AI Project Bot, URL redirect |
| v4.0.0 | **Security review response** (7 findings) — `users/` read locked to admin + own; `access/` and `commercialAccess/` reads scoped; client-side bootstrap removed (manual admin seed); `provisionUser` Cloud Function for sign-in; admin callables (`adminApprove`/`Deny`/`Delete`/`SetRole`/`SetProjectAccess`/`SetCommercialAccess`) with **audit log** on all sensitive ops; URL validation (https-only, `javascript:`/`data:`/`file:` blocked). **Hardware manual override** (HubSpot value = suggestion; Instrumental users can override per-field, override wins in Demand Plan). **Project Overview** section with 8 fields — CAD Complete, CAD Actual Finish, Actual Service Start, Target Build, Actual Deploy (webapp source of truth) + Target Build at Deal Close + CS Program ID (HubSpot pull-only) + Project Status/Next Steps (Bot-drafted). **AI-drafted Project Status** button wires to existing Project Bot. Folds in uncommitted v3.2.0 + v3.3.0 + Apr 22 sign-in hotfixes. |
| v4.0.1 | **HubSpot Sync history log** (Admin Panel → HubSpot Sync) — every sync (manual or scheduled) writes an entry to `hubspotSync/log/` with type, actor, state, counts, duration; rendered as a table in the admin UI. **SI Kanban now driven by HubSpot** — added "SI Partner Deployment" pipeline (ID `2206979797`) with 8 stages (SIRD → DFM → Quote → PO → Build → FAT → SAT → Live). Projects in this pipeline auto-populate `siStage` from HubSpot's stage on every sync. SI Kanban filters by pipeline membership (no longer by `[SI]` name pattern), so `[SI]` projects in Hardware Deployment Pipeline stay in Hardware. `hubspotSync/.read` tightened to admin-only. |
| v4.0.2 | **Hotfixes**: (1) `writeSyncLogEntry` and `writeAuditEntry` were building DB keys from ISO timestamps (containing `.`), which Firebase Realtime Database forbids in path segments. Both now use `db.ref(...).push(entry)` — Firebase auto-generates path-safe, time-sortable keys. (2) HubSpot sync apply errored with `set failed: value argument contains undefined in property '...siStage'` for legacy projects (synced before SI Partner pipeline support) where both `incoming_p.siStage` and `merged[idx].siStage` were missing — coerced fallback to `?? null` and added `sanitizeForFirebase()` recursive helper applied to project writes (defensive guard against any future undefined-value writes). (3) Admin panel copy updated from "6 HubSpot pipelines" → "7" to reflect the SI Partner Deployment addition. |
| v4.0.4 | **HubSpot sync bug fixes**: (1) "Last sync Never" — UI read `syncStatus.lastSync`/`.count` but CF writes `syncedAt`/`total`; field names corrected. (2) Preview always showed "2 projects found" — `Object.values(data)` on `{ projects, summary }` returned 2 entries; fixed to `Object.values(data.projects \|\| {})`. (3) "Error: internal" on any sync failure — `manualHubspotSync` propagated plain JS errors as opaque Firebase "internal"; now wrapped in try/catch that rethrows with real message. (4) Hardware tracking not populating — `fetchAllStationKits` used v3 inline association key `2-39524389` which doesn't reliably map for custom-to-custom objects; replaced with `fetchKitsForProjects` using v4 batch associations API (project → kit direction). **Validation tab**: FAT/SAT status fields now have × clear buttons (Instrumental only) to reset back to "Not started". |
| v4.1.0 | **Major feature release** — 9 workstreams shipped: (1) **Sidebar sub-nav**: all Project Details categories listed as collapsible sub-items; external users scoped to 3 folders. (2) **Non-PDF uploads**: DOCX/XLSX/PPTX/images/CSV/.lbx allowed; external users read-only. (3) **Gantt toggle**: "📊 Show Gantt" button, disabled <3 dates. (4) **HubSpot project links**: 🔗 on project names in 4 views. (5) **Pre-uploaded deployment docs**: 10 system docs auto-linked per project via `ensureProjectTemplate` CF. (6) **`si_admin` role + All SI Projects view**: editable SI tracker (risk, contact, milestone, notes), SI Kanban moved here, sub-item under All Projects Overview. (7) **HubSpot writeback**: date field saves PATCH HubSpot custom object; `getHubspotCustomObjectSchema` diagnostic CF; fail-safe local save. (8) **Scheduled maintenance CF** (Tue/Fri 3 PM PT): sweeps old logs, evaluates 3 agentic rules (bug count, sync error rate, circuit breaker auto-pause); Admin Panel → 🔧 Maintenance tab. (9) **Performance**: code-split vendor chunks (react/firebase), memoization, function memory bumps. |
| v4.5.8 | **`pd_meeting_notes` (and `pd_cad`) now auto-appear on every project, not just new ones or ones with HubSpot data.** Pre-v4.5.8, the Meeting Notes tab only showed up for projects that had at least one HubSpot Note already (the sync auto-created the folder when it found notes to insert). Users wanted to author notes from the app for *any* project, including those with no inbound HubSpot data yet. Fix applies the same triple-path consistency that `pd_cad` already had: (1) `STANDARD_FOLDER_TEMPLATES` in `src/App.jsx` now includes `pd_meeting_notes` so the client-side merge in `getProjectDetails` injects it on every project view; (2) `ensureProjectTemplate` CF allow-list now adds both `pd_cad` and `pd_meeting_notes` if missing (server-side backfill on project open); (3) `backfillDeploymentDocs` CF allow-list also adds both (admin-triggered backfill for all projects). Backfill executed against all 791 projects after deploy to materialize the folders in RTDB. **Process note for future folder additions**: any new standard folder/category must be added to all 3-4 auto-inject paths (template + STANDARD_FOLDER_TEMPLATES + ensureProjectTemplate + backfillDeploymentDocs), not just `buildProjectDetails`, otherwise it won't appear on existing projects. |
| v4.5.7 | **Meeting Notes — bidirectional sync with HubSpot Notes engagement objects.** New `pd_meeting_notes` category (type: "notes") added to the project template; renders as a reverse-chronological timeline in the new `NotesSection` component. **Inbound** (`functions/index.js` `fetchNotesForProjects` + runSync block after CAD Release): on each sync, for each project, batch-read `POST /crm/v4/associations/projects/note/batch/read` to get associated note IDs, batch-read note properties (`hs_note_body`, `hs_timestamp`, `hubspot_owner_id`, `hs_attachment_ids`), batch-fetch attachment metadata from `GET /files/v3/files/{id}` (name + signed URL + mime), strip HTML body to plain text (preserves `<br>` + `</p>` as newlines), resolve author via `fetchAllOwners`, merge into `pd_meeting_notes.items` using the same allow-list pattern as v4.5.5 CAD docs (match by hidden `_hubspotNoteId`, preserve manual items, respect `_userDeleted` soft-delete, auto-create folder if missing). **Outbound** (new `writeNoteToHubspot` CF): client posts `{projectHubspotId, body, attachments?:[{name, base64, mimeType}]}`; CF uploads each attachment via `POST /files/v3/files` multipart to `/deployment-portal-notes/{projectHubspotId}/{timestamp}/` (private), creates the note via `POST /crm/v3/objects/notes` with `hs_note_body` (plain text wrapped in `<p>`), `hs_timestamp`, `hs_attachment_ids` (semicolon-joined), `hubspot_owner_id` (best-effort matched to caller's email via `/crm/v3/owners?email=`), then associates note→project via `PUT /crm/v4/objects/notes/{id}/associations/default/{OBJECT_TYPE}/{projectHubspotId}`. All steps logged to `hubspotWriteback/log`. **Frontend `NotesSection`**: timeline view (newest first), each note card shows author + timestamp + HUBSPOT badge + plain-text body with auto-detected URL hyperlinks + inline image previews (attachments with mime `image/*`) + chip links for non-image attachments + ✕ to soft-delete. "✚ Add note" opens an inline editor with textarea + file picker; **clipboard paste handler** intercepts pasted images and routes them through the same attachment upload path. Optimistic UI: posts immediately with `_pendingId` + ↑ indicator, swaps to `_hubspotNoteId` + ✓ on success, ⚠ on error. **Internal-only** — `pd_meeting_notes` not in EXTERNAL_VISIBLE. Wired into routing at `ProjectTabsView` (renders `NotesSection` when `cat.type === "notes"`), added to TAB_ORDER + STANDARD_CAT_IDS. Files API scope verified on the HubSpot token before coding. Bundles previously-deployed v4.5.5 CAD Release sync + v4.5.6 pd_specs removal under the same release. |
| v4.5.6 | **`pd_specs` folder removed from non-SI projects; existing items migrated to `pd_cad`.** With v4.5.5 routing CAD documentation through the new HubSpot CAD Release sync into `pd_cad`, the legacy `pd_specs` ("Design Specifications & Integration Docs") folder is redundant for non-SI deployment projects. Migration: `functions/checklists.js` `buildProjectDetails` now wraps `pd_specs` in an `isSI` conditional (SI projects keep it); client-side `DEFAULT_PROJECT_DETAILS` ([src/App.jsx:158](src/App.jsx#L158)) and `STANDARD_FOLDER_TEMPLATES` ([src/App.jsx:484](src/App.jsx#L484)) no longer include `pd_specs`. One-shot RTDB migration applied 2026-06-22 via `firebase database:update`: 781 non-SI projects had `pd_specs` removed; 779 had empty folders (just removed), 2 had real items that were appended to `pd_cad` (tagged `migratedFrom: "pd_specs"` + `migratedAt`). All 5 SI projects left untouched (still have `pd_specs`). `EXTERNAL_VISIBLE` / `TAB_ORDER` / `STANDARD_CAT_IDS` left as-is (no-op for projects without `pd_specs`; still relevant for SI). |
| v4.5.5 | **HubSpot CAD Release → `pd_cad` folder auto-sync.** HDEs use a HubSpot workflow to release CAD packages: it emails the team and also writes 4 URLs onto Project custom properties (`cad_folder`, `station_overview_hde`, `station_bom_details_hde`, `machining_notes_hde`). Pre-v4.5.5 the webapp's "CAD & Drawings" folder was manual-only — the team had to context-switch to email to find the links. Now every sync reads those 4 properties and creates/updates 4 corresponding link items inside `pd_cad`: `CAD Folder`, `Station Overview`, `Station BOM Details`, `Machining Notes`. Same allow-list merge pattern as v4.5.2 `pd_station_kits` and v4.5.3 `pd_team`: items match by new hidden `_hubspotSource` key; user-added items (no `_hubspotSource`) preserved; user soft-delete (`_userDeleted: true`) respected so the next sync won't resurrect deleted items; blank HubSpot URL → leave existing item alone; changed HubSpot URL → update in place with `updatedAt` timestamp. Frontend (`src/App.jsx` `FolderSection`): new purple **HUBSPOT** badge under each synced item's name (parallel to the existing `SYSTEM` badge); `delItem` extended to soft-delete `source === "hubspot"` items via `_userDeleted` so deletion sticks across syncs. Zero changes to the HubSpot workflow or HDE process. New `CAD_DOC_DEFS` const (`functions/index.js`) defines the 4 slots; `cadDocs: {...}` exposed on the project record by `mapHubspotToProject`; new sync block in `runSync` after the DRI block, before shipments. Verified end-to-end on `[Deploy] Nerds / Farglory TW / (2 Set) VR - FA Station - Bay B & Bay C` (HsId `316447023841`). |
| v4.5.4 | **4 bundled changes**: completed-project access toggle, AI bot model fix, and 3 bug-sweep fixes. **(1) Completed/inactive project access.** Pre-v4.5.4, projects whose HubSpot stage closed (`hubspotStageClosed: true` at [functions/index.js:312](functions/index.js#L312)) had their `status` flipped to `"inactive"` by sync, after which they disappeared from the sidebar project dropdown — even though all underlying RTDB data (projectDetails, _hardwareTracking, pd_team, pd_station_kits, files, checklists) was preserved intact. Inactive projects were unreachable from the UI; users couldn't reference past deployments. **Fix**: new "Include completed (N)" checkbox in the sidebar (admin/instrumental only, surfaces below the project search input — only renders when at least one inactive project exists). Toggle persists in sessionStorage as `v454_show_inactive` (resets per session to avoid surprise). Inactive entries in the dropdown render at reduced opacity with a `📦 completed` italic suffix. Selecting one navigates to the project page normally; a yellow banner appears at the top of the Overview tab — "Completed project — read-only reference. The HubSpot stage is closed. Edits still write back to HubSpot if needed." All existing writeback paths remain functional, so users can still correct historic data via the same callable CFs. Zero data migration; zero backend changes — pure UI surfacing of data that was already preserved. **(2) AI Bot 404 fix.** All 5 Cloud Functions calling Anthropic (`chatBot`, `askProjectBot`, `askGlobalBot`, `aiSIParseTimelineImport`, `aiSIParseCoverageDoc`) were using deprecated model names — `claude-sonnet-4-20250514` (3 CFs) and `claude-3-5-sonnet-20241022` (2 CFs) — both no longer in Anthropic's model list, returning 404. All 5 updated to `claude-sonnet-4-6` (current stable Sonnet). Verified via direct API smoke test. **(3) Security fix: `adminDeleteUser` now also deletes the Firebase Auth user.** Pre-v4.5.4 only wiped `users/{uid}` + `access/` + `commercialAccess/` from RTDB; the Firebase Auth account persisted, letting a "deleted" user re-authenticate and trigger `provisionUser` to recreate their record. Now also calls `admin.auth().deleteUser(uid)` with try/catch (Auth user may not exist for legacy RTDB-only records); audit log records both `authDeleted` boolean + any error. **(4) Code hygiene fix: `actor` declaration in `useApp` moved above the SI auto-create `useEffect`.** Previously declared at App.jsx:5198 but referenced at 5162/5184/5186 inside a useEffect defined at 5142. Worked via closure timing (useEffect runs after commit, by which point `const actor` is bound) but was fragile + missing from the deps array. Now declared upfront + included in deps. **(5) `writeShipmentToHubspot` no longer silently falls through to CREATE on search failure.** Previously a try/catch around the "search by INxxx" step had an empty catch, falling through to POST — would silently create a duplicate Shipment record on transient HubSpot 5xx (HubSpot doesn't enforce uniqueness on `shipment_tracking_number`). Now logs to `hubspotWriteback/log` and throws `unavailable` so the user can retry. |
| v4.5.3 | **pd_team auto-populates 4 DRI rows from HubSpot Owners (inbound).** The DRI section in HubSpot exposes 4 enumeration properties on the Project object (`hde__hardware_design_engineer_`, `sie__software_integration_engineer_`, `sa_solutions_architect`, `cs_customer_success`) — each stores a HubSpot Owner ID. v4.5.3 fetches all HubSpot Owners once per sync via `/crm/v3/owners?limit=100` (paginated), resolves each project's 4 DRI Owner IDs to person details (`firstName lastName`, `email`), and writes 4 fixed rows into `pd_team` per project: `HDE (Hardware Design Engineer)`, `SIE (Software Integration Engineer)`, `SA (Solutions Architect)`, `CS (Customer Success)`. Rows match by new hidden `_driRole` field; the merge step overwrites only `role`/`name`/`email` and preserves user-edited `company`/`location`/`phone`/`description`. **Manually-added team rows (no `_driRole`) are never touched** — sync only manages the 4 fixed roles. When a DRI is unset in HubSpot, the row still renders with empty name/email (signals "no one assigned"). Writeback FROM pd_team cells is deferred — users still edit DRIs directly in HubSpot. New helpers: `fetchAllOwners`, `buildTeamRowsFromDRIs`, `DRI_ROLES` const. Properties added to project property fetch list. |
| v4.5.2 | **pd_station_kits table now auto-populates from HubSpot sync (inbound).** Pre-v4.5.2 the 63-column `pd_station_kits` table inside projectDetails was manual-entry only — users had to type fixture names, computer SNs, camera SNs, lens SNs etc. by hand even though all that data already existed in HubSpot Kit + Fleet Asset records. Now every sync writes one row per kit, populating 16 HubSpot-derived columns: `fixture_name` (from kit `station_kit_sn`), `station_name`, `computer_sn`, `computer_service_tag`, `mac_address` (from Computer-labeled Fleet Asset properties incl. `product_sn_service_tag` and `mac_address__c`), `camera_1_sn`, `lens_1_sn`, `barcode_scanner_sn`, `monitor_sn`, `led_controller_sn`, and boolean presence flags (`cameras_present`, `lenses_present`, `barcode_scanner`, `monitor`, `leds`) + `no_cameras` count. Rows match by new hidden `_kitHubspotId` field; existing rows merge only the HubSpot-driven keys via an explicit allow-list — all ~48 manual columns (keyboard, mouse, cables, brackets, nest fields, notes, ship_date, etc.) are **preserved untouched** across syncs. New kits append fresh rows; rows with no HubSpot match stay as-is (full manual data). `fetchComponentsForKits` extended to read `product_sn_service_tag` + `mac_address__c` on each fleet asset. Writeback from `pd_station_kits` cells deferred to a future release — users still have the v4.5.0 HardwareTrackingSection UI for SN/kit-field writeback. Only slot 1 populated this release (Camera 1, Lens 1); 2-5 stay app-only until column schema expands. |
| v4.5.1 | **CRITICAL data-safety fix: `save()` field-level diff writes.** Pre-v4.5.1, the client-side `save()` in `src/App.jsx` wrote the **entire** `appState/docData/${pid}` node on every state change. If `state.docData[pid]` was momentarily partial — during a sync's multi-path update, or after a buggy UI's rapid re-render swallowed state — the write replaced the full RTDB node with a partial snapshot, silently wiping fields like user-edited table rows. **Incident**: 2026-06-11, Sunnyhills GB300 lost `pd_team` rows during v4.5.0 testing (the nested-component remount bug in HardwareTrackingSection tripped the race; that UI bug also fixed in this release). **Fix**: `save()` now does a per-key diff inside `docData/${pid}` and writes only the sub-keys whose reference actually changed. Untouched fields stay in RTDB regardless of what's in state. Also bundles the HardwareTrackingSection nested-component fix (parent-level edit state) and the `writeFleetAssetAssociation` archive endpoint fix (DELETE single-association instead of broken `batch/archive` body shape). Recommended follow-up: enable Firebase Console → Realtime Database → Backups for daily snapshots. |
| v4.5.0 | **Stage 6B — Station Kit + Fleet Asset HubSpot writeback (bidirectional).** Two new Cloud Functions in `functions/index.js`: (1) `writeFleetAssetAssociation` — when a user edits a fleet asset SN in the app's Hardware Tracking section, the CF searches HubSpot for a Fleet Asset with the new SN, then **PUT-then-DELETE** re-points the Kit↔FleetAsset associations to the new asset. PUT new (labeled + unlabeled typeId 35) first so a partial failure never leaves a kit slot without an associated asset; old links are best-effort archived after. Errors with friendly "create the Fleet Asset first" if no match found. (2) `writeStationKitToHubspot` — direct PATCH on the Kit object for 5 fields: `name`, `station_kit_sn`, `computer_sn`, `status`, `station_type`. Sync changes ([functions/index.js:457-547](functions/index.js#L457-L547)): `fetchComponentsForKits` now captures each association's labeled `typeId` (e.g. "Camera 1", "Lens 3") via `associationTypes` parsing; `buildHardwareFromKits` propagates `_kitHubspotId`, `_fleetAssetHubspotId`, `_associationLabel` plus full kit-level fields (`kitName`, `computerSN`, `kitStatus`, `stationType`) onto each row. HubSpot Kit↔FleetAsset associations are slotted (Camera 1-5, Lens 1-5, Computer, LED Light Controller); UI now groups rows by kit and shows the actual slot label per row. New consts: `FLEET_ASSET_TYPE_ID = "2-39261140"`, `FLEET_ASSET_LABEL_TO_TYPE_ID` map (19 labels). **UI**: `HardwareTrackingSection` ([src/App.jsx:2853](src/App.jsx#L2853)) rewritten — per-kit grouped layout with inline-editable kit-level fields, per-row inline-editable serial cells for the 4 writable association labels (Camera, Lens, Computer, LED Light Controller), per-row ↑/✓/⚠ writeback indicator, **first-edit confirm prompt** for kit-field edits (sessionStorage flag) to mitigate catastrophic mistakes. Manual items split into their own group, app-only (never written back). Bidirectional: inbound HubSpot→app already worked for fleet asset SN; v4.5.0 sync extension also surfaces kit-level fields so HubSpot-side edits flow back on next sync. |
| v4.4.1 | **HubSpot ↔ app shipment sync hardening (two fixes bundled).** (1) **Inbound: direct Project↔Shipment associations now respected.** Pre-v4.4.1, `fetchShipmentsViaKits` only walked Project → Kit → Shipment, so any shipment associated directly to a Project (HubSpot association typeId `129`, `2-39524389` ↔ `2-39524475`) was silently dropped from sync. Discovered while joint-testing Stage 6A: `[Deploy] Nerds Wistron D1` had `IN00790` linked directly to the project, not via either kit — confirmed via direct HubSpot API calls (project `309607375576` → 2 kits with 0 shipment assocs each → 1 direct shipment `311506300638`). New helper `fetchShipmentsDirect` (`functions/index.js`) mirrors `fetchShipmentsViaKits`'s `{id, num}` return shape and uses the same batched v4 associations + v3 properties pattern with `fetchWithTimeout`. `runSync` now calls both paths in parallel and dedup-unions by INxxx so shipments are picked up regardless of which association path the user used in HubSpot. Pure additive read-path change — no schema/writeback impact. (2) **Outbound: `item_num` rename PATCH bug fixed.** Pre-v4.4.1, `writeShipmentToHubspot` built its PATCH body from `HUBSPOT_SHIPMENT_PROPS`-mapped fields only — `item_num` (→ `shipment_tracking_number`) was passed in separately but never assembled into the PATCH body, so item_num-only edits hit the "no mapped fields changed → skipped: true" early return at `functions/index.js:1543` and silently no-op'd while showing ✓ in the UI. Fix: always include `shipment_tracking_number: item_num.trim()` in the properties body (idempotent — HubSpot no-ops if unchanged), remove the skip early-return, simplify create vs. update reqBody to a single shape. Discovered during Step C of v4.4.1 joint test. |
| v4.4.0 | **Stage 6A — Shipment Details HubSpot writeback (create-or-update)**. New `writeShipmentToHubspot` Cloud Function patches the HubSpot Shipments custom object (`2-39524475`) when a user edits a `pd_shipment_details` row. If the row has `hubspotShipmentId` (set by sync), PATCH that record. If not, search HubSpot by `shipment_tracking_number` (INxxx); patch if found, otherwise CREATE a new Shipment with the new INxxx + properties. 5 writable fields confirmed via schema diagnostic: `contents` → `shipment_contents__c`, `carrier` → `logistics_company__c`, `tracking_num` → `internal_shipment_id` (carrier tracking #), `ship_date` → `date_shipped__c`, `notes` → `notes`. Local-only fields (box_size, weight) never written back. **Sync change**: `fetchShipmentsViaKits` now returns `{id, num}` pairs; `runSync` shipment block persists `hubspotShipmentId` on each row and backfills existing rows by matching INxxx on the next sync. **UI**: per-row `↑/✓/⚠` indicator next to the delete button on `TableSection` when `cat.id === "pd_shipment_details"`; hover ⚠ to see HubSpot error. Errors also recorded in the existing Audit Log. |
| v4.3.4 | **HubSpot date writeback finally working** — fixed latent bug in `writeProjectDateToHubspot` where dates were being sent as noon UTC ms (`T12:00:00Z`) when HubSpot requires strict midnight UTC. Bug existed since v4.1.0 but was masked by the missing-scope 403 errors. Once IT granted `crm.objects.custom.write` and `HUBSPOT_TOKEN` was rotated, the underlying `INVALID_DATE` error surfaced. One-line fix at `functions/index.js:1314` (`T12:00:00Z` → `T00:00:00Z`). All read + write scopes verified across Custom Objects, Standard Objects, Schemas, Pipelines, Lists, and Files. Stage Kanban writebacks (non-SI + SI) are independent of date format and unaffected. |
| v4.3.3 | **"Order Fan" → "Order Form" propagation fix** — External Checklist `ext_0_1` and `ext_ms_0` description were typo'd as "Order Fan". `CHECKLIST_PATCHES` extended with new `labelRewrites` (conditional on matching the old buggy label) and `newDescription`/`oldDescription` rewrite — only swaps if the value still matches the old template, so user-customized labels/descriptions are preserved. Existing projects pick up the corrected wording on next page load; user-edited items are untouched. **MES Integration Setup Checklist PDF added to `DEPLOYMENT_REQUIREMENTS_FOLDER`** as a new system reference doc (`doc_mes_setup_checklist`) at `templates/deployment_requirements/MES Integration Setup Checklist.pdf`. Auto-injected into every project's "Hardware & MES Deployment Requirements" folder via `ensureProjectTemplate` (new projects) and `backfillDeploymentDocs` (existing projects, admin-triggered). |
| v4.3.2 | **Internal Checklist content update** (Deployment Checklist Consolidated v1.2) — 5 new items added across milestones 1, 3, and 4 (final config confirmation, optics/hardware compatibility, mounting design finalization, computer provisioning, Lastpass + Station SOP setup). Existing projects auto-inject the new items via `CHECKLIST_PATCHES` on next page load; projects with user-customized checklists keep their existing items untouched (additive-only). Source-of-truth labels in `functions/checklists.js` updated for new projects (line-visit-by-TPM in mounting validation, calibration boards in extra hardware, buffer language in daily schedule). **Ownership field inline-editable** — the `ck.ownership` field on each checklist task is now click-to-edit for Instrumental users (mirrors the existing label and notes edit patterns). Empty ownership shows a "· + owner" placeholder; populated ownership is clickable. No UI layout changes — same position, same styling. Uses the v4.0.3 functional-updater pattern so rapid edits don't race. |
| v4.3.1 | **Audit Log browser** (Admin Panel → 🔍 Audit Log) — surfaces both the User Action Log (`auditLog/`) and the HubSpot Writeback Log (`hubspotWriteback/log/`) so admins can see actual HubSpot error responses when writebacks fail. Filter by action type or writeback status. **Slack feedback** — new "💬 Send Feedback" button at the bottom of the sidebar opens a modal that routes SI feedback to Sneha's channel and everything else to Asang's, via new `sendSlackFeedback` CF + `SLACK_WEBHOOK_ASANG` / `SLACK_WEBHOOK_SNEHA` secrets. **Schema diagnostic now multi-object** — Admin Panel → HubSpot Sync → Schema buttons for Projects, Shipments, and Station Kits (custom object types `2-39524389`, `2-39524475`, `2-39260531`); `getHubspotCustomObjectSchema` CF now accepts `objectTypeId` param. |
| v4.3.0 | **Non-SI Kanban drag-and-drop stage writeback** — Instrumental users can drag project cards between stage columns in All Projects Overview → Kanban mode; drop writes the new stage to HubSpot via new `writeStageToHubspot` CF (`PATCH /crm/v3/objects/${OBJECT_TYPE}/${hubspotId}`) with optimistic update + revert-on-error. SI pipeline excluded (not in `PIPELINE_LIST`). Error banner shows on writeback failure. **Remove SI from main project dropdown** — admin's sidebar project combobox now filters out SI Partner Deployment pipeline projects; they remain accessible via All SI Projects and All Projects Overview. |
| v4.2.9 | **MES Integration Checklist** — new sub-tab under Project Details for all projects (standard and SI), sourced from `MES_Integration_Setup_Checklist_external.docx`; 9 milestone sections (Network & Connectivity, SSL Certificate, API Endpoints, Station Names & Route Config, Auth & Credentials, Test Environment, Error Handling, Data Format & Serial Numbers, Go-Live Readiness) with ~70 checkbox items; check-off records timestamp and owner. Checklist auto-injected client-side for existing projects via `APP_MES_CHECKLIST_TEMPLATE` in `getProjectDetails`; `backfillChecklists` CF also picks it up for server-side persistence. **Webapp Project ID field** — new Instrumental-only editable field in Project Overview (not visible to external users); stored in `projectOverview` alongside other date fields. **Tab reorder sidebar fix** — sidebar sub-nav now sorts by `TAB_ORDER` (matching the main tab bar). |
| v4.2.8 | **Checklist check-off audit** — checking a task now records `checkedBy` (user name) and `checkedAt` (timestamp); displayed below the task label in teal as "✓ Name · Jan 1, 2026 at 9:30am". Unchecking clears both fields. **Add Row/Station at top** — `TableSection` and `TransposedTableSection` now show `+ Add Row` / `+ Add Station` + `📥 Import` buttons above the table (in addition to below) when there are existing rows, so users don't have to scroll down on long tables. **Pipeline Kanban in All Projects Overview** — "Projects by Stage" section now has a ☰ List / ⬛ Kanban toggle; Kanban mode shows one column per pipeline stage with color-coded project cards (customer, station count, SI flag); switching pipelines updates the Kanban live. |
| v4.2.7 | **Fix sync hang — `fetchWithTimeout` helper** — all HubSpot API calls in the sync path (`fetchAllHubspotObjects`, `discoverStationObjectTypes`, `fetchKitsForProjects`, `fetchComponentsForKits`, `fetchShipmentsViaKits`) now time out at 15s via a shared `fetchWithTimeout` wrapper using `AbortController`. Previously, any single silent HubSpot API hang could consume the full 300s function window before any log output appeared, causing every sync to time out. **Fix hardware docs** — system docs in "Hardware & MES Deployment Requirements" folder now open via `getDownloadURL()` on click (resolves a download-token URL the browser can follow without auth headers), replacing the old `<a href="?alt=media">` approach which required Firebase Auth in the request header. `checklists.js` updated to store `storagePath` instead of `url` for new projects; legacy items with `url` field handled via path extraction fallback. Dead code removed from `App.jsx` (`storageBaseUrl`, `APP_DEPLOY_REQ_FOLDER`). |
| v4.2.6 | **Fix shipment sync hang** — rewrote `fetchShipmentsViaKits`: HubSpot has no direct Project→Shipment association; shipments are linked to Station Kits. Old code queried a non-existent association endpoint that hung indefinitely (no `node-fetch` timeout), causing every apply sync to time out at 300s. New code traverses Kit→Shipment using kit IDs already fetched by the station kits block, with 15s AbortController timeouts on every fetch call. Also hoisted `kitsByProject` to share it between both blocks. **Fix Hardware docs 404** — storage bucket URL in `checklists.js` was `deploymentportal-5ec3a.appspot.com` (non-existent) instead of `deploymentportal-5ec3a.firebasestorage.app`; uploaded all 10 docs to the correct bucket path. **Fix client deadline-exceeded** — both `manualHubspotSync` callable invocations now pass `{ timeout: 300000 }` to match the 300s server timeout. |
| v4.2.5 | **Fix sync OOM + timeout** — `manualHubspotSync` and `scheduledHubspotSync` bumped to `memory: "8GB", timeoutSeconds: 300` (was 512 MB / 60s default); fixes "Error: Internal" caused by OOM crash and 60s timeout when syncing 305+ projects with station kits + shipments. Eliminated redundant 3rd full docData read in `runSync` (station kits snapshot now shared with shipments block, saving one Firebase round trip and one in-memory copy). |
| v4.2.4 | **Error: Internal fix** — wrapped `runSync` secondary ops (preview clear, status write, log entry) in individual try/catch so a post-sync Firebase write failure no longer surfaces as "Error: Internal" to the user; sync data was already persisted. **`listHubspotSchemas` CF** — new admin diagnostic that calls `GET /crm/v3/schemas` and returns all custom object type IDs, names, and properties; surfaced as "🗂️ All HubSpot Object Types" button in Admin Panel → HubSpot Sync tab (used to discover the Shipments object). **Shipment Details HubSpot sync** — each HubSpot sync now also batch-reads project → Shipments associations (object type `2-39524475`, property `shipment_tracking_number`), and upserts the INxxx numbers as new rows in each project's Shipment Details tab (`item_num` only, no duplicates, leaves all other columns blank for manual fill-in). |
| v4.2.3 | **Station Kits complete attribute list** — expanded from 35 to 63 columns to match full Excel layout. Added to Cameras: `cameras_present` bool, `no_camera_brackets`. Added to Lenses: `lenses_present` bool. Added to Lights: UV/white light rotational notch #s, no. LED cables/length, LED cables bool, Wordop automated/manual controller bools, UV light intensity, LEDC power cable, 4x LED controller ext. cables. New **Station Components** section (16 rows): Station SN, Andon/Stack Light+Bracket, Keyboard/Mouse Tray, Monitor Bracket, Barcode Scanner Mount, Cable Ties & Wrap, Baseboard/Mainboard 3D/CNC nests, Power Converter (Brazil) 2x, Instrumental Sign, NBR cable, NEMA adapter, Brazil power cord. Mirrored in `functions/checklists.js`. No migration needed — additive columns only. |
| v4.0.3 | **Critical UX fix — Checklists**: previous `ChecklistSection` had a Rules of Hooks violation (`useState` inside an IIFE inside a conditional render). When milestones expanded/collapsed, React's hook order shifted between renders → state corruption → infinite re-render loops → Chrome tab crashes (and one user reported the tab recovering into a Google search). Rewritten cleanly: each task is now a `<button type="button">` row with checkbox + label, no broken hooks. **Editable checklists** — Instrumental users can now `+ Add task` per milestone (inline input) and delete tasks via the × button. Add/delete tasks persist to `docData/{pid}/projectDetails`. **PDF uploads to Firebase Storage** — new `PdfUploadButton` component wired into the three file-add forms (Project Details, Commercial, Training). Files go to `gs://<bucket>/uploads/{projectId}/...` (50 MB max, content-type enforced). New `storage.rules` gates writes to authenticated users + PDF-only + size cap; client-side validator mirrors the same limits. **Strict-SI**: `isSI` is now ONLY true for projects in the SI Partner Deployment pipeline. **Dead-code purge**: removed ~558 lines of unreachable v3.x components (`DocsView`, `MilestoneCard`, `SIValidation`, `SIHardware`, `ProgramDetails`) that referenced the long-deleted `PARTY_DEFS` constant. **Sync log polish**: "Recent errors" panel filters to last 24h. **SI Kanban barricade**: blue gradient panel with "SI PARTNER DEPLOYMENT PIPELINE" label tab. **Standalone checklist backfill**: new `backfillChecklists` Cloud Function + Admin Panel button applies templates to all projects missing them — no HubSpot sync required. Multi-path Firebase update batched in chunks of 100; client timeout bumped to 9 minutes. |

---

## Documentation

- [HOW_TO_USE_GUIDE_4.1.0.md](HOW_TO_USE_GUIDE_4.1.0.md) — End-user guide (current)
- [HOW_TO_USE_GUIDE_4.0.3.md](HOW_TO_USE_GUIDE_4.0.3.md) — End-user guide (previous)
- [SECURITY_REVIEW_4.0.0.md](SECURITY_REVIEW_4.0.0.md) — Response to 4/24 security review
- [REBUILD_4.0.0.md](REBUILD_4.0.0.md) — Step-by-step guide to rebuilding the project from scratch
- [PRE_DEPLOY_RUNBOOK_4.0.0.md](PRE_DEPLOY_RUNBOOK_4.0.0.md) — Admin seed instructions + pre-deploy test checklist

---

## Security

See [SECURITY_REVIEW_4.0.0.md](SECURITY_REVIEW_4.0.0.md) for the current security assessment.
